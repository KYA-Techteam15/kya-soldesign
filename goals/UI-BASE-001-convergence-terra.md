# Goal UI-BASE-001 Convergence — truthful closure

## Objective

Complete Phase 6 convergence tasks T052–T058 for roadmap feature `UI-BASE-001`, reconcile the original T012–T051 evidence honestly, and close the validated bilingual UI foundation only after the implementation, visual matrix, accessibility journeys, `pnpm verify`, and `$speckit-converge` are all green.

## Authority and scope

1. Work only in this autonomous repository.
2. Respect `AGENTS.md`, `PRODUCT.md`, `.specify/memory/constitution.md`, `ROADMAP.md`, and every artifact under `specs/002-validated-ui-foundation/`.
3. The validated visual authority remains commit `1be343f814abc6379517389c42e484806634e6bb`, light/sober, using only fresh `reference-captures/*-live.png`. Never use the old parent `shots/` directory.
4. Execute `specs/002-validated-ui-foundation/tasks.md` Phase 6 T052–T058 in order. Reconcile T012–T051 only from real named files and evidence; do not mass-check incomplete tasks.
5. Do not implement any production calculation, database, persistence, Tauri, cloud, live weather download, simulated result, or AIO-001 formula.
6. Parent design and legacy sources are read-only audit inputs and must remain absent from the production import graph.

## Required implementation outcomes

- Canonical locality/weather-source selection and `@ksd/domain`-validated load inputs in dedicated workshop step modules.
- Complete truthful Equipment and Dossier intent surfaces without compatibility, sizing, protection, finance, or report results.
- Explicit localized unknown-route/project/segment and recoverable application-port error states.
- Complete shared Dialog/Flow primitives, removal of unused abstractions, and focused keyboard/focus/status tests.
- Complete typed FR/EN structure, hard-coded-copy scan, `<html lang>` checks, and dual-locale route/draft preservation matrix.
- Complete named E2E cases and the full FR/EN visual matrix at 1440×1000 and 1024×768, including palette/dialog and workshop surfaces.
- Truthful requirements, visual, convergence, tasks, and roadmap evidence.

## Execution contract

1. Use `$ksd-spec-audit` and `$speckit-analyze` before implementation.
2. Work task-by-task with failing tests first where the named evidence is absent.
3. Run `$ksd-ui-acceptance` after visible changes and inspect captures against the live reference.
4. Run `$ksd-phase-gate` and `corepack pnpm verify:phase`; do not continue on red.
5. At closure run the production-boundary and i18n scanners, Impeccable detector, full `corepack pnpm test:visual`, and `corepack pnpm verify`.
6. Run `$speckit-converge`; implement any newly appended tasks and repeat until it reports clean convergence.
7. Set `UI-BASE-001` to `done` only after every remaining task and checklist item has evidence. Do not begin `AIO-001` in this goal.

## Completion report

Return the completed task IDs, named source/test/capture artifacts, FR/EN key and route counts, accessibility/responsive/truth-state results, production-boundary result, visual difference ledger, phase/final gate results, convergence result, and confirmation that no parent source or AIO calculation was modified.
